{
  "name": "smscan",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cargo watch -c -w src -w templates -x 'run -p smscan'",
    "build": "cargo build -p smscan --release",
    "test": "cargo test -p smscan"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-plugin-tailwindcss": "^0.3.0",
    "tailwindcss": "^3.3.2"
  }
}
